#!/bin/bash

# replace ' -' with '-', '- ' with '-', ',' with '_', any number of spaces or underscores with one underscore
for f in "$@" ; do
    newname="$(echo -n $f | sed -e 's/ \+-/-/g' -e 's/- \+/-/g' -e 's/ \+/_/g' -e 's/,/_/g' -e 's/_\+/_/g')"
    mv "$f" "$newname"
done
