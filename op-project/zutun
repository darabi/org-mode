

- toggle words und letters in das Menü
- format paragraph ins Menü
- hide leaves ins Menü



bei indent am Ende eines Wortes dabbrev
             (define-key cdg-mode-map [tab] 'indent-relative)
             (define-key cdg-mode-map [(meta tab)] 'dabbrev-expand)
             (make-variable-buffer-local 'font-lock-maximum-size)
             (setq font-lock-maximum-size 50000)
             (turn-on-auto-fill)
             (iso-accents-mode)


Lernen: was mach gnuserv-start ( jetzt im .emacs)




- Datum Statuszeile in Deutsch

- indent-Erweiterung auf *E etc.?

- help mit allen Zeilen bei der Funktionshilfe

  [- nach Verweis bleibt das Overlay des Verweises erkennbar (BUG oder Feature?)
    -> Feature]

- M-q reorganisiere den Paragraf



fine-ediff hat Probleme mit dem Overlay-Modus des XEmacs (müssen hier extents verwendet werden??)

Kalender hat keine Farbvorlage (eher nicht so wichtig, wäre aber nützlich)

Wiedervorlage-System in den Kalender einbauen
Planungssystem einbauen:
	Es gibt den Fall, dass einzelne Punkte nur "Brainstorming"-Punkte sind,
	d.h. diese sind noch keine reellen Projektpunkte, aber Teile davon werden
	schon verwirklicht und delegiert
Delegationssystem dokumentieren
M-c TAB etc. kommentieren

Bei Wort-Suche (Pattern): Abtrennung des Wortes durch ' 
	->Dekanat'es findet Dekanat (vielleicht auch Dekanat_es? s. nächsten Satz)
Helfsdokumentation: Peter_Pollok findet Peter_Pollok und Peter Pollok

Ruler und M-C Tab etc. muss in das Menü, ebenso Uppercase etc. M-S-c etc.

Beim Ausgliedern [mit Mail] noch nach cvs-checkin fragen für File .cvs.op

Logbuch-Mode OHNE use...: File mit .crypt sogt nicht für gecryptete Endversion
	(crypting wird ignoriert), Vorbereitungen bei "; noch in Arbeit"


search-this-string-forward muss so umgeschrieben werden, dass es den kompletten String
  auch mit Bindestrich etc. sucht


